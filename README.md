This is a demo for an SSI integration using NextJs

To get started follow these steps:

```bash
npm install
npm run dev
```
Navigate to [http://localhost:3000/api/header](http://localhost:3000/api/header)
